version: "3"
services:
    test:
        build:
            context: .
            dockerfile: Dockerfile.test
        environment:
            TESTING: true
        volumes:
            - ./server:/usr/src/app/server
            - ./data:/usr/src/app/data
